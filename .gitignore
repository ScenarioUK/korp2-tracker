# Secrets — never commit. UI_TOKEN, MCP_TOKEN and DATABASE_URL are env vars only.
.env
.env.*
!.env.example

# Build output
/dist/

# Dependencies
node_modules/

# Local portable Postgres: binaries and cluster data. Machine-local, never
# committed. Removing local dev entirely is `rm -rf tools/`.
/tools/

# Logs / OS noise
*.log
npm-debug.log*
.DS_Store
Thumbs.db
